<% include partials/header %>
 
    <h1>Ovo je lista dostupnih igrica:</h1>
    <!--
    <div class="row">
    <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Pretrazi</button>
    </form>
    </div>
    -->
    <div class="row">
    
        <% for(var i = 0;i < gamesList.length; i++){ %>
        <div class="col-md-4">
            <div class="card" style="width: 18rem;">
                <img class="card-img-top" src="games/thumbnails/<%= gamesList[i].thumbnailFile %>" style="height: 180px">
                <div class="card-body">
                <h5 class="card-title"><%= gamesList[i].title %></h5>
                <p class="card-text">Autor je <%= gamesList[i].creator %></p>
                <a href="/game/<%= gamesList[i].id %>" class="btn btn-primary">Igraj</a>
                <a href="/game/edit/<%= gamesList[i].id %>" class="btn btn-primary">Izmeni</a>
                <a href="/game/obrisi/<%= gamesList[i].id %>" class="btn btn-danger">Obrisi</a>
                </div>
            </div>
        </div>
        <% } %>
    </div>
    
<% include partials/footer %>